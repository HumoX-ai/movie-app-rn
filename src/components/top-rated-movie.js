import React from "react";
import { Dimensions, Text, View } from "react-native";

export const TopRatedMovie = ({ upcoming, title }) => {
  return (
    <View>
      <Text className={"text-white"}>TopRatedMovie</Text>
    </View>
  );
};
